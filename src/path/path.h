#pragma once

#include <stddef.h>

int spot_save_path(char* save_path, char* file_name);
int spot_type(char* type, char* file_name);
int spot_subject(char* subject, char* file_name);
int countdir(char *dir);
